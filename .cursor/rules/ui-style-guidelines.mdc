---
description: 统一 CloudCreate Workspace 交互控件样式，避免再次出现不一致按钮或内联色值
globs: src/**/*.svelte
alwaysApply: false
---

# UI/交互样式规范

- **优先复用公共样式**  
  - 交互按钮、分页/缩放等操作控件必须复用已有的 `control-btn` 或其他既有 `.app-header-*` / `.app-sidebar-*` 类。  
  - 不允许在组件内部随意添加新的按钮类名或内联 `style` 来定义 padding/圆角/背景；如确需扩展，应在公共样式文件（如 `src/app.css`）补充通用类并在此文件中记录。

- **只能使用 CCW 主题变量**  
  - 所有前景/背景颜色、边框、阴影必须引用 `--ccw-*` 变量（例如 `var(--ccw-text-secondary)`），不得直接写 `#fff`、`rgba(...)` 等魔法值。  
  - 若组件需要渐变或阴影，请先确认 `src/app.css` 是否已有 `--ccw-shadow-soft` 等变量；没有时新增变量再引用。  
  - **[app.css](src/app.css) 由 Vite+Tailwind 当标准 CSS 处理，禁止使用 Svelte 的 `:global(...)` 包装**；该写法在管道中会被丢弃，导致整段全局样式（含 `.btn`、布局类、主操作区）不进入构建产物。全局样式用普通类选择器即可。

- **交互状态完整**  
  - 新增控件必须包含 `:hover`、`:disabled` 等状态，动画/过渡统一使用 `transition: … 150ms ease`。  
  - 禁止只实现默认态导致主题切换或悬停反馈缺失。

- **组件复用示例**  
  - 分页、缩放、清空等操作按钮 → 使用 `<button class="control-btn">` 并根据需要添加 `disabled`。  
  - 进度/加载反馈 → 统一使用 `ProgressBar` 组件或 Skeleton 现有指示器，不得自定义临时进度条。  
  - 工具页主操作行（如「处理 / 转换」+「清空」成对出现）→ 使用 `<section class="workspace-primary-actions">` 包裹；**不含**子级下边距的卡片内行可加 `workspace-primary-actions--tight`。主按钮为 `btn preset-filled-primary-500`、次按钮为 `btn preset-outlined-surface-200-800`；**勿** 对表格内 `btn-sm` 使用本容器。全站主按钮（含工作区行内非 `btn-sm`）在 [app.css](src/app.css) 中统一为 **VS Code 风**：扁平填色、1px 细描边、`--ccw-radius-button`（3px）圆角、无渐变/外发光；`workspace-primary-actions` 仅再约定约 28px 行高与行内 padding。表格/紧凑区仍用 `btn btn-sm preset-filled-primary-500`，样式与上同源。
